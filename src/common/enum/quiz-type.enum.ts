export enum QuizTypeEnum {
  UserQuiz = 'UserQuiz', //no pre-defined questions. questions are fetched based on requested filter
  Standard = 'Standard', //quiz questions to be added at time of creation
}
