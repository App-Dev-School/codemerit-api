require('dotenv').config();
export const jwtConstants = {
    //secret: process.env.JWT_SECRET,
    JWT_REFRESH_SECRET: "skillTest@2025",
    secret: "skillTest@2025"
};
export const LOG = 'CodeMeritLog';